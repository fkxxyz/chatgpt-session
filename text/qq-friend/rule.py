from typing import List

from memory import Message


def compile_history(messages: List[Message], params: dict) -> (str, List[Message]):
    assert False  # TODO 未实现


def compile_message(message: Message) -> str:
    assert False  # TODO 未实现
